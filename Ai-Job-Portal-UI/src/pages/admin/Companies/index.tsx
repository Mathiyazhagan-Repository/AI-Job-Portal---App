import { Component as Moderation } from '../Moderation'

/**
 * A5 — Company verification queue.
 * Same screen as A6 with the companies tab selected; the two queues share
 * one review surface rather than duplicating the decision UI.
 */
export const Component = Moderation
